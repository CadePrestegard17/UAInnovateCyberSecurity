# Render Blueprint: backend only. Dashboard is on Netlify.
services:
  - type: web
    runtime: python
    name: uainnovatecybersecurity
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
